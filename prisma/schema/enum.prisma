enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  USER
  ADMIN
  LIBRARIAN
}

enum Shift {
  MORNING
  EVENING
}

enum BookStatus {
  AVAILABLE
  RESERVED
  OUT_OF_STOCK
}

enum authProvider {
  GOOGLE
  CREDENTIAL
}

enum BookingStatus {
  PENDING
  APPROVED
  REJECTED
  ISSUED
  RETURNED
  OVERDUE
  CANCELLED
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  REFUNDED
}

enum PaymentType {
  RENT
  FINE
}

