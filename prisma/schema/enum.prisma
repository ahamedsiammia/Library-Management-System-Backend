enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  USER
  ADMIN
  LIBRARIAN
}

enum Shift {
  MORNING
  EVENING
}

enum BookStatus {
  AVAILABLE
  RESERVED
  OUT_OF_STOCK
}

enum authProvider {
  GOOGLE
  CREDENTIAL
}
